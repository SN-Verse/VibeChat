# Client environment example
# Copy to .env in the client root and fill values

# Vite requires VITE_ prefix to expose to the client
VITE_BACKEND_URL=http://localhost:5001
VITE_SOCKET_SERVER_URL=http://localhost:5001
